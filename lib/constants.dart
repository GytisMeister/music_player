import 'package:flutter/material.dart';

// Color Presets for all cases
const kPrimaryColor = Color(0xFFFFFFFF);
const kSecondaryColor = Color(0xFFBABABA);
const kAccentColor = Color(0xFF31ff6a);
const kScaffold = Color(0x00000000);

// Preset duration for animations
const kAnimationDuration = Duration(milliseconds: 200);
